<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dataagg.account.mapper.AccountMapper">
	<resultMap id="BaseResultMap" type="com.dataagg.account.domain.Account">
		<id column="id" property="id" jdbcType="BIGINT"/>
		<result column="user_name" property="username" jdbcType="VARCHAR"/>
		<result column="password" property="password" jdbcType="VARCHAR"/>
	</resultMap>

	<sql id="Base_Column_List">
		id, user_name, password
	</sql>

	<select id="getAll" resultMap="BaseResultMap">
		SELECT
		<include refid="Base_Column_List"/>
		FROM sys_users
	</select>

	<select id="getOne" parameterType="java.lang.Long" resultMap="BaseResultMap">
		SELECT
		<include refid="Base_Column_List"/>
		FROM sys_users
		WHERE id = #{id}
	</select>

	<select id="getByName" parameterType="java.lang.String" resultMap="BaseResultMap">
		SELECT
		<include refid="Base_Column_List"/>
		FROM sys_users
		WHERE user_name = #{username}
	</select>

	<insert id="insert" parameterType="com.dataagg.account.domain.Account">
		INSERT INTO
		sys_users
		(user_name,password)
		VALUES
		(#{user_name}, #{password})
	</insert>

	<update id="update" parameterType="com.dataagg.account.domain.Account">
		UPDATE
		sys_users
		SET
		<if test="user_name != null">user_name = #{user_name},</if>
		<if test="password != null">password = #{password},</if>
		WHERE
		id = #{id}
	</update>

	<delete id="delete" parameterType="java.lang.Long">
		DELETE FROM
		sys_users
		WHERE
		id =#{id}
	</delete>
</mapper>