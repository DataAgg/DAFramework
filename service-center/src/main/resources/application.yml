spring:
  application:
    name: service-center
server:
  port: 8761
logging:
  level:
    root: WARN
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://service-center:8761/eureka/
